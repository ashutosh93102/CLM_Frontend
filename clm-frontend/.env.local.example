# Copy this file to `.env.local` and fill values

# Google OAuth (frontend)
# Create credentials in Google Cloud Console (OAuth client ID for Web)
# Add authorized JavaScript origins: http://localhost:3000
# Add authorized redirect URIs if you use redirect-based flows (this app uses Google Identity Services)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Backend base URL (optional)
# Defaults to http://127.0.0.1:11000 if omitted
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:11000
